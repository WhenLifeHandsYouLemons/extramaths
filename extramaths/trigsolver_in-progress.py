# Imports
import math

# Trigonometry solver
def right_angle_trig(hypotenuse, opposite, adjacent, angle):
	if hypotenuse == 0:
	    print(hypotenuse)
	if opposite == 0:
	    print(opposite)
	if adjacent == 0:
	    print(adjacent)
	if angle == 0:
	    print(angle)

right_angle_trig(43, 23, 45, 4)
